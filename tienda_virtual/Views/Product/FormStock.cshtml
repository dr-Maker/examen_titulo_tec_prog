
@{
    @model List<ProductModel>
    List<CategoryModel> ListCategory = ViewBag.CategoryList;
    Layout = "~/Views/Shared/_layout.cshtml";
}

<div class="w-100 height">
    <h1 class="txt-center">Modificar Stock</h1>

    <form class="form" method="post">
        <div class="d-flex">
            <label class="f-label">Producto</label>
            <input class="f-input" type="text" name="category" readonly />
        </div>
        <div class="d-flex">
            <label class="f-label">Stock</label>
            <input class="f-input" type="number" name="category" />
        </div>
        <div class="d-flex">
            <label class="f-label">Tipo de Talla</label>
            <select class="f-input" >
                <option>seleccione</option>
            </select>
        </div>
        <div class="d-flex">
            <label class="f-label">Talla</label>
            <select class="f-input">
                <option>seleccione</option>
            </select>
        </div>
        <div>
            <input class="btn_new mg-top-30" type="submit" value="Registrar categoria" />
        </div>

    </form>

</div>